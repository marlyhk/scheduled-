{
  "name": "scheduled",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp index.html styles.css app.js vercel.json README.txt public/ && cp -r assets public/assets"
  }
}